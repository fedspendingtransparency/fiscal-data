import React from 'react';

const FilterButtons = ({ handleApply, handleCancel }) => {
  //TODO: Filter buttons can be reused from report filter dropdown
  return <>Placeholder for filter buttons</>;
};

export default FilterButtons;
