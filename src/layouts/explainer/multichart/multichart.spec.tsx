describe('Multichart', () => {

  it('passes the expected arguments to MultiChart renderer.', () => {
    return;
  });
  it('renders a group of svg descendant elements for each chart config stacked vertically', () => {
    return;
  });
})
